apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${PVC_NAME}
spec:
  storageClassName: "${STORAGE_CLASS}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
